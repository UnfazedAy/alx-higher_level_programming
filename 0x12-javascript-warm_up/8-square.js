t { argv } = require('procress');
const num = parseInt(argv[2]);
let result = ''
if (isNaN(num) {
	  console.log('Missing size');
} else {
	  for (let i = 0; i < num; i++); {
		      for (let j = 0; j < num; j++);
		        console.log(`X + ${result}`);
		    }
	    console.log(result)
}
